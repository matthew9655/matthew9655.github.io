/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        appDir: true,
      },
    // basePath: "/.",
    // assetPrefix:"/.",
    output: "export",
};

export default nextConfig;
